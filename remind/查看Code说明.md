# 🔍 如何查看微信 Code 码

## ✅ 已完成配置

我已经为你添加了调试模式，可以直接获取真实的微信 code！

### 配置说明（第 42 行）

```typescript
const debugMode = true  // 设置为 true 可查看真实的微信 code
```

---

## 📱 查看 Code 的步骤

### 1. 打开微信开发者工具

确保已经导入项目，AppID 为：`wx7b965f99d207b7f3`

### 2. 编译项目

点击左上角的 **"编译"** 按钮

### 3. 点击"微信一键登录"

在模拟器中点击"微信一键登录"按钮

### 4. 查看 Code

会弹出一个对话框显示：

```
获取到微信 Code

071xxxxxxxxxxxxx

有效期：5分钟
长度：32 位

[复制]  [关闭]
```

**同时在控制台也会打印**：
```
🔍 调试模式：获取真实微信 code
✅ 微信 Code: 071xxxxxxxxxxxxx
```

---

## 📋 Code 的特点

- **格式**: 071 开头的字符串
- **长度**: 通常 32 位
- **有效期**: 5 分钟
- **一次性**: 每次点击都会生成新的 code
- **用途**: 需要发送给后端换取用户的 openid

---

## 🔄 模式切换

### 当前配置（第 41-42 行）

```typescript
const isDev = true       // 模拟登录开关
const debugMode = true   // 查看 code 开关
```

### 三种模式

| 模式 | isDev | debugMode | 说明 |
|------|-------|-----------|------|
| **查看 Code** | true | **true** | 获取真实 code 并显示（当前） |
| **模拟登录** | **true** | false | 使用假数据登录 |
| **真实登录** | **false** | false | 需要后端支持 |

---

## 💡 Code 的工作流程

```
前端                          后端                    微信服务器
  │                            │                         │
  │ 1. 点击"微信一键登录"        │                         │
  │ ──────────────────────────────────────────────────> │
  │                            │                         │
  │                            │    2. 返回 code         │
  │ <────────────────────────────────────────────────── │
  │                            │                         │
  │ 3. 发送 code 到后端         │                         │
  │ ─────────────────────────> │                         │
  │                            │                         │
  │                            │ 4. 用 AppID + AppSecret │
  │                            │    + code 换取 openid   │
  │                            │ ─────────────────────> │
  │                            │                         │
  │                            │ 5. 返回 openid          │
  │                            │ <───────────────────── │
  │                            │                         │
  │ 6. 返回 token 和用户信息    │                         │
  │ <───────────────────────── │                         │
  │                            │                         │
```

---

## 🧪 测试用例

### 正常情况

**控制台输出**：
```
🔍 调试模式：获取真实微信 code
✅ 微信 Code: 071abc123def456...
```

**弹窗显示**：
```
获取到微信 Code
071abc123def456...
有效期：5分钟
长度：32 位
```

### 点击"复制"按钮

会自动复制 code 到剪贴板，并提示：
```
Code 已复制 ✓
```

---

## ❓ 常见问题

### Q1: 没有弹窗显示？
**检查**：
- `debugMode = true` 是否设置正确
- 是否点击了"编译"按钮
- 查看控制台是否有错误

### Q2: 获取到的 code 是 undefined？
**原因**：微信授权失败
**解决**：
1. 检查 AppID 是否正确
2. 确保在微信开发者工具中测试
3. 查看详情 → 基本信息中的 AppID

### Q3: Code 每次都不一样？
**正常现象**！每次调用 `uni.login` 都会生成新的 code

### Q4: 想关闭调试模式？
修改第 42 行：
```typescript
const debugMode = false  // 改为 false
```

---

## 🎯 下一步

拿到 code 后，你可以：

1. **测试后端接口**
   - 把 code 手动发送给后端
   - 验证后端能否正确解析

2. **查看 code 格式**
   - 了解 code 的样子
   - 用于调试和测试

3. **准备真实登录**
   - 当后端准备好后
   - 设置 `debugMode = false` 和 `isDev = false`

---

**最后更新**: 2025-01-18

