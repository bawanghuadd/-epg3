# 🗺️ 路由配置说明

## 📋 当前路由配置

### 已配置的页面

| 路径 | 说明 | 导航栏 | 下拉刷新 |
|------|------|--------|---------|
| `pages/index/index` | 登录页 | 自定义 | 否 |
| `pages/home/home` | 首页 | 自定义 | 是 |

---

## 🎯 路由配置文件位置

**文件**: `pages.json`

这是 uni-app 的路由配置文件，所有页面路由都在这里配置。

---

## 📱 当前配置详解

```json
{
  "pages": [
    {
      "path": "pages/index/index",       // 页面路径
      "style": {
        "navigationBarTitleText": "登录", // 导航栏标题
        "navigationStyle": "custom",      // 自定义导航栏
        "enablePullDownRefresh": false    // 禁用下拉刷新
      }
    },
    {
      "path": "pages/home/home",
      "style": {
        "navigationBarTitleText": "首页",
        "navigationStyle": "custom",      // 自定义导航栏
        "enablePullDownRefresh": true     // 启用下拉刷新
      }
    }
  ],
  "globalStyle": {
    "navigationBarTextStyle": "black",
    "navigationBarTitleText": "工程师上门维修",
    "navigationBarBackgroundColor": "#FFFFFF",
    "backgroundColor": "#F5F5F5",
    "navigationBarHeight": "44px"
  }
}
```

---

## ➕ 如何添加新页面

### 方法 1：在 HBuilderX 中创建（推荐）

1. 右键点击 `pages` 文件夹
2. 选择 **新建 → 页面**
3. 输入页面名称（如 `order`）
4. 勾选 **在pages.json中注册**
5. 点击创建

**自动完成**：
- ✅ 创建页面文件
- ✅ 自动添加到 `pages.json`
- ✅ 添加基础配置

---

### 方法 2：手动配置

#### 步骤 1：创建页面文件

```
pages/
  ├── order/
  │   └── order.uvue      ← 新建
```

#### 步骤 2：在 pages.json 中注册

```json
{
  "pages": [
    {
      "path": "pages/index/index",
      "style": {...}
    },
    {
      "path": "pages/home/home",
      "style": {...}
    },
    {
      "path": "pages/order/order",    // ← 新增
      "style": {
        "navigationBarTitleText": "订单",
        "navigationStyle": "default"
      }
    }
  ]
}
```

---

## 🧭 路由跳转方式

### 1. uni.navigateTo（保留当前页面）

```typescript
// 跳转到订单页面，可以返回
uni.navigateTo({
  url: '/pages/order/order'
})

// 带参数跳转
uni.navigateTo({
  url: '/pages/order/order?id=123&status=paid'
})

// 接收参数
onLoad((options) => {
  console.log(options.id)      // 123
  console.log(options.status)  // paid
})
```

### 2. uni.redirectTo（关闭当前页面）

```typescript
// 跳转到新页面，关闭当前页，无法返回
uni.redirectTo({
  url: '/pages/home/home'
})
```

### 3. uni.reLaunch（关闭所有页面）

```typescript
// 关闭所有页面，打开新页面
// 登录成功后常用
uni.reLaunch({
  url: '/pages/home/home'
})
```

### 4. uni.switchTab（切换 Tab 页面）

```typescript
// 只能跳转到 tabBar 页面
uni.switchTab({
  url: '/pages/home/home'
})
```

### 5. uni.navigateBack（返回）

```typescript
// 返回上一页
uni.navigateBack()

// 返回两层
uni.navigateBack({
  delta: 2
})
```

---

## 📊 路由跳转对比

| 方法 | 关闭当前页 | 可返回 | 页面栈 | 适用场景 |
|------|----------|--------|--------|----------|
| `navigateTo` | ❌ | ✅ | 增加 | 普通跳转 |
| `redirectTo` | ✅ | ❌ | 替换 | 不需要返回 |
| `reLaunch` | ✅全部 | ❌ | 清空重建 | 登录/首页 |
| `switchTab` | ✅ | ❌ | 切换 | Tab切换 |
| `navigateBack` | ✅ | - | 减少 | 返回 |

---

## 🎨 导航栏配置

### navigationStyle 选项

```json
// 1. 默认导航栏（显示标题）
"navigationStyle": "default"

// 2. 自定义导航栏（完全自定义）
"navigationStyle": "custom"
```

### 你的当前配置

```json
"navigationStyle": "custom"  // 使用自定义导航栏
```

**效果**：
- ✅ 顶部没有默认导航栏
- ✅ 可以完全自定义样式
- ✅ 你的登录页和首页都用了自定义

---

## 🔄 路由守卫（权限控制）

### 在页面中检查登录状态

```typescript
// pages/order/order.uvue
import { requireLogin } from '@/utils/auth'

onMounted(() => {
  // 检查是否登录，未登录自动跳转登录页
  if (!requireLogin()) {
    return
  }
  
  // 已登录，加载数据
  loadOrderData()
})
```

### 全局路由拦截（App.vue）

```typescript
// App.uvue
export default {
  onLaunch() {
    console.log('App Launch')
  },
  
  onShow() {
    // 应用切回前台时检查登录状态
    const token = uni.getStorageSync('token')
    if (!token) {
      const pages = getCurrentPages()
      const currentPage = pages[pages.length - 1]
      
      // 如果不在登录页，跳转到登录页
      if (currentPage.route !== 'pages/index/index') {
        uni.reLaunch({
          url: '/pages/index/index'
        })
      }
    }
  }
}
```

---

## 📝 常用页面路由示例

### 订单相关

```json
{
  "path": "pages/order/order",
  "style": {
    "navigationBarTitleText": "我的订单"
  }
},
{
  "path": "pages/order/detail",
  "style": {
    "navigationBarTitleText": "订单详情"
  }
}
```

### 个人中心

```json
{
  "path": "pages/profile/profile",
  "style": {
    "navigationBarTitleText": "我的",
    "navigationStyle": "custom"
  }
},
{
  "path": "pages/profile/settings",
  "style": {
    "navigationBarTitleText": "设置"
  }
}
```

### 服务相关

```json
{
  "path": "pages/service/list",
  "style": {
    "navigationBarTitleText": "服务列表"
  }
},
{
  "path": "pages/service/detail",
  "style": {
    "navigationBarTitleText": "服务详情"
  }
}
```

---

## 🎯 底部导航栏说明

### 为什么没有配置 tabBar？

你的项目使用了**自定义底部导航栏**（在 `home.uvue` 中实现）：

**优点**：
- ✅ 样式完全自定义
- ✅ 中间可以有悬浮按钮
- ✅ 更灵活美观

**缺点**：
- ❌ 需要在每个 Tab 页面复制代码
- ❌ 切换时会重新加载页面

### 如果需要原生 tabBar

在 `pages.json` 中添加：

```json
"tabBar": {
  "color": "#666666",
  "selectedColor": "#E53935",
  "backgroundColor": "#FFFFFF",
  "list": [
    {
      "pagePath": "pages/home/home",
      "text": "首页"
    },
    {
      "pagePath": "pages/order/order",
      "text": "订单"
    }
  ]
}
```

**但注意**：需要移除 home.uvue 中的自定义底部导航！

---

## 🚀 最佳实践

### 1. 首页放在第一个

```json
{
  "pages": [
    {
      "path": "pages/index/index"  // 第一个是启动页
    }
  ]
}
```

### 2. 页面分类组织

```
pages/
  ├── index/          # 入口页（登录）
  ├── home/           # 首页
  ├── order/          # 订单相关
  │   ├── list/
  │   └── detail/
  ├── service/        # 服务相关
  └── profile/        # 个人中心
```

### 3. 统一命名规范

```
pages/模块名/功能名
pages/order/list      ✅
pages/order/detail    ✅
pages/orderlist       ❌
```

---

## 📖 完整示例

需要我帮你添加更多页面吗？比如：
- 订单页面
- 个人中心
- 消息页面
- 设置页面

告诉我需要哪些，我来帮你配置！

---

**配置文件**: `/Users/daren1/Desktop/epg3/pages.json`

