# 📂 文件夹排序规范指南

本文档展示了项目文件夹的最规范排序方式。

---

## 🌟 IDE 中的显示顺序

在 VS Code、HBuilderX 等 IDE 中，文件夹会按以下规则自动排序：

### 排序规则
1. **文件夹优先于文件**
2. **字母/数字顺序**（不区分大小写）
3. **特殊符号在前**（. _ -）

---

## 📋 标准显示顺序（IDE 自动排序）

```
epg3/
├── 📁 .vscode/                    # 点号开头（隐藏配置）
├── 📁 api/                        # A 开头
├── 📁 components/                 # C 开头
├── 📁 composables/                # C 开头
├── 📁 config/                     # C 开头
├── 📁 docs/                       # D 开头
├── 📁 mixins/                     # M 开头
├── 📁 pages/                      # P 开头 ⭐ 最常用
├── 📁 plugins/                    # P 开头
├── 📁 static/                     # S 开头
├── 📁 store/                      # S 开头
├── 📁 styles/                     # S 开头
├── 📁 types/                      # T 开头
├── 📁 uni_modules/                # U 开头
├── 📁 unpackage/                  # U 开头（忽略）
├── 📁 utils/                      # U 开头
├── 📄 .gitignore                  # 点号开头文件
├── 📄 App.uvue                    # A 开头
├── 📄 index.html                  # I 开头
├── 📄 main.uts                    # M 开头
├── 📄 manifest.json               # M 开头
├── 📄 package.json                # P 开头
├── 📄 pages.json                  # P 开头 ⭐ 重要配置
├── 📄 platformConfig.json         # P 开头
├── 📄 project.config.json         # P 开头
├── 📄 README.md                   # R 开头 ⭐ 项目说明
├── 📄 sitemap.json                # S 开头
├── 📄 theme.json                  # T 开头
└── 📄 uni.scss                    # U 开头
```

---

## 🎯 推荐的逻辑排序（使用前缀）

如果想要自定义排序，可以使用数字或下划线前缀：

### 方案一：数字前缀（推荐）

```
epg3/
├── 📁 01-pages/                   # 页面 - 第一优先级
├── 📁 02-components/              # 组件 - 第二优先级
├── 📁 03-styles/                  # 样式
├── 📁 04-utils/                   # 工具
├── 📁 05-config/                  # 配置
├── 📁 06-api/                     # 接口
├── 📁 07-store/                   # 状态
├── 📁 08-static/                  # 静态资源
├── 📁 09-docs/                    # 文档
└── 📁 10-uni_modules/             # 插件
```

**优点**：
- ✅ 符合开发流程顺序
- ✅ 最常用的在最上面
- ✅ 一目了然

**缺点**：
- ❌ 文件夹重命名工作量大
- ❌ 不符合社区习惯
- ❌ 引用路径需要包含数字

### 方案二：下划线前缀（次推荐）

```
epg3/
├── 📁 _pages/                     # 下划线会排在前面
├── 📁 _components/
├── 📁 components/                 # 正常命名的在后面
├── 📁 config/
└── 📁 utils/
```

**优点**：
- ✅ 可以突出重要文件夹
- ✅ 不需要数字

**缺点**：
- ❌ 不符合命名规范
- ❌ 部分工具可能识别为私有文件夹

### 方案三：自然排序（最推荐）⭐

使用首字母自然排序，通过命名设计达到合理顺序：

```
epg3/
├── 📁 components/                 # C - 组件（高频）
├── 📁 composables/                # C - 组合函数
├── 📁 config/                     # C - 配置
├── 📁 pages/                      # P - 页面（高频）
├── 📁 plugins/                    # P - 插件
├── 📁 services/                   # S - 服务层（替代 api/）
├── 📁 static/                     # S - 静态资源
├── 📁 store/                      # S - 状态管理
├── 📁 styles/                     # S - 样式
├── 📁 utils/                      # U - 工具函数
└── 📁 views/                      # V - 视图（如果不用 pages）
```

**优点**：
- ✅ 符合社区规范
- ✅ 无需重命名
- ✅ 路径简洁
- ✅ 大多数项目都这样

---

## 📊 各目录重要性评分

| 目录 | 使用频率 | 重要性 | 建议位置 |
|------|---------|--------|---------|
| `pages/` | ⭐⭐⭐⭐⭐ | 🔴 极高 | 靠前 |
| `components/` | ⭐⭐⭐⭐⭐ | 🔴 极高 | 靠前 |
| `styles/` | ⭐⭐⭐⭐ | 🟠 高 | 中间 |
| `utils/` | ⭐⭐⭐⭐ | 🟠 高 | 中间 |
| `config/` | ⭐⭐⭐ | 🟡 中 | 中间 |
| `api/` | ⭐⭐⭐⭐ | 🟠 高 | 中间 |
| `store/` | ⭐⭐⭐ | 🟡 中 | 中间 |
| `static/` | ⭐⭐⭐ | 🟡 中 | 靠后 |
| `docs/` | ⭐⭐ | 🟢 低 | 靠后 |
| `uni_modules/` | ⭐ | 🟢 低 | 最后 |
| `unpackage/` | ⭐ | 🟢 低 | 忽略 |

---

## 🎨 IDE 中的文件夹图标优化

### VS Code 推荐插件

1. **Material Icon Theme**
```json
// settings.json
{
  "workbench.iconTheme": "material-icon-theme",
  "material-icon-theme.folders.associations": {
    "pages": "views",
    "components": "components",
    "utils": "tools",
    "config": "config",
    "api": "api",
    "store": "redux-store",
    "styles": "sass"
  }
}
```

2. **file-icons**
- 自动识别文件类型
- 显示对应图标
- 更易区分

### 文件夹排序插件

**Explorer Exclude**
```json
// settings.json
{
  "explorer.sortOrder": "type",
  "explorer.fileNesting.enabled": true,
  "explorer.fileNesting.patterns": {
    "*.uvue": "${capture}.wxml, ${capture}.js"
  }
}
```

---

## 📁 子目录排序规范

### pages/ 目录

按照业务模块和使用频率：

```
pages/
├── index/              # 登录/首页 - 最先访问
├── home/               # 主页
├── order/              # 订单 - 核心业务
│   ├── order.uvue     # 列表
│   ├── create.uvue    # 创建
│   └── detail.uvue    # 详情
├── message/            # 消息
├── profile/            # 个人中心
├── service/            # 服务
└── engineer/           # 工程师端
```

### components/ 目录

按照功能类型：

```
components/
├── common/             # 通用组件
│   ├── Button.uvue
│   ├── Input.uvue
│   └── Modal.uvue
├── layout/             # 布局组件
│   ├── NavBar.uvue
│   └── TabBar.uvue
├── business/           # 业务组件
│   ├── OrderCard.uvue
│   └── UserInfo.uvue
└── feedback/           # 反馈组件
    ├── Loading.uvue
    └── Empty.uvue
```

### utils/ 目录

按照功能和依赖关系：

```
utils/
├── request.js          # 基础 - 请求封装
├── auth.js            # 基础 - 认证
├── validator.js       # 基础 - 验证
├── errorHandler.js    # 基础 - 错误处理
├── api.js             # 应用 - API 接口
├── navigation.js      # 应用 - 导航
├── common.js          # 应用 - 通用工具
└── index.js           # 统一导出（可选）
```

### static/ 目录

按照资源类型：

```
static/
├── icons/              # 图标
│   ├── tabbar/        # 底部导航图标
│   ├── home/          # 首页图标
│   └── common/        # 通用图标
├── images/             # 图片
│   ├── banner/        # 轮播图
│   ├── product/       # 产品图
│   └── avatar/        # 头像
├── fonts/              # 字体
└── logo.png            # Logo
```

---

## 🔍 文件夹命名速查表

### ✅ 推荐命名

| 目录名 | 说明 | 别名 |
|--------|------|------|
| `pages/` | 页面 | `views/` |
| `components/` | 组件 | `comps/` |
| `utils/` | 工具函数 | `helpers/` |
| `api/` | API 接口 | `services/` |
| `store/` | 状态管理 | `stores/` |
| `config/` | 配置文件 | `configs/` |
| `styles/` | 样式文件 | `css/`, `scss/` |
| `static/` | 静态资源 | `assets/` |
| `types/` | 类型定义 | `typings/` |
| `composables/` | 组合函数 | `hooks/` |
| `mixins/` | 混入 | - |
| `plugins/` | 插件 | - |

### ❌ 不推荐命名

| 不推荐 | 原因 | 应该用 |
|--------|------|--------|
| `js/` | 不明确 | `utils/` |
| `common/` | 太泛泛 | 具体功能名 |
| `public/` | 易混淆 | `static/` |
| `src/` | uni-app 不需要 | 直接在根目录 |
| `lib/` | 易混淆 | `utils/` |

---

## 🎯 最终推荐方案

### 标准项目结构（自然排序）

```
epg3/
│
├── 📦 配置文件（根目录）
│   ├── App.uvue
│   ├── main.uts
│   ├── pages.json
│   ├── manifest.json
│   └── ...
│
├── 📁 源码目录（按字母排序）
│   ├── api/                       # A
│   ├── components/                # C ⭐
│   ├── composables/               # C
│   ├── config/                    # C
│   ├── mixins/                    # M
│   ├── pages/                     # P ⭐⭐⭐
│   ├── plugins/                   # P
│   ├── store/                     # S
│   ├── styles/                    # S ⭐
│   ├── types/                     # T
│   └── utils/                     # U ⭐
│
├── 📁 资源目录
│   ├── static/                    # 静态资源
│   └── uni_modules/               # uni 插件
│
└── 📁 文档目录
    └── docs/                      # 文档
```

### 核心原则

1. **不使用数字前缀** - 保持命名自然
2. **遵循社区规范** - 使用通用命名
3. **合理利用首字母** - 让常用目录靠前
4. **保持结构扁平** - 避免过深嵌套
5. **分类清晰明确** - 每个目录职责单一

---

## 🚀 快速操作

### VS Code 文件夹收起/展开

```json
// keybindings.json
[
  {
    "key": "cmd+k cmd+0",
    "command": "workbench.files.action.collapseExplorerFolders"
  }
]
```

### 快速定位文件

- `Cmd/Ctrl + P` - 快速打开文件
- `Cmd/Ctrl + Shift + F` - 全局搜索
- `Cmd/Ctrl + Click` - 跳转到定义

---

## 📝 检查清单

- [ ] 文件夹按字母顺序自然排列
- [ ] 常用目录容易找到
- [ ] 命名符合社区规范
- [ ] 无不必要的嵌套
- [ ] IDE 显示美观
- [ ] 团队成员都能理解

---

**选择最适合您团队的方案，保持一致性最重要！** ✨

