# 后端接口需求文档

## 📋 概述

本文档列出了项目需要的所有后端接口，按照功能模块进行分类。

---

## 🔐 1. 登录认证模块

### 1.1 微信登录
**接口地址**: `POST /miniapp/login/wechat`

**请求参数**:
```json
{
  "code": "微信登录code",
  "nickname": "用户昵称（可选）",
  "avatar": "用户头像URL（可选）",
  "role": 1  // 用户角色：1-客户，2-工程师
}
```

**返回数据**:
```json
{
  "code": 0,
  "data": {
    "token": "JWT token",
    "userId": "用户ID",
    "nickname": "用户昵称",
    "avatar": "头像URL",
    "phone": "手机号",
    "userType": "customer|engineer",
    "isVerified": false,
    "role": 1,
    "isFirstLogin": false  // 是否首次登录
  },
  "msg": "登录成功"
}
```

### 1.2 手机号登录
**接口地址**: `POST /miniapp/login/phone`

**请求参数**:
```json
{
  "phoneCode": "手机号授权码",
  "wechatCode": "微信登录code",
  "userInfo": {
    "nickName": "昵称",
    "avatarUrl": "头像"
  }
}
```

**返回数据**: 同微信登录

---

## 👤 2. 用户信息模块

### 2.1 获取用户信息
**接口地址**: `GET /user/info`

**请求头**: `Authorization: Bearer {token}`

**返回数据**:
```json
{
  "code": 0,
  "data": {
    "userId": "用户ID",
    "nickname": "昵称",
    "avatar": "头像URL",
    "phone": "手机号",
    "userType": "customer|engineer",
    "isVerified": false,
    "role": 1,
    "vipLevel": 0
  }
}
```

### 2.2 更新用户信息
**接口地址**: `PUT /user/info`

**请求头**: `Authorization: Bearer {token}`

**请求参数**:
```json
{
  "nickname": "新昵称",
  "avatar": "新头像URL",
  "phone": "新手机号"
}
```

**返回数据**: 同获取用户信息

### 2.3 获取用户统计数据
**接口地址**: `GET /user/stats`

**请求头**: `Authorization: Bearer {token}`

**返回数据**:
```json
{
  "code": 0,
  "data": {
    "totalAmount": "1,280.00",  // 账户余额
    "coupons": 5,  // 优惠券数量
    "points": "2,680"  // 积分
  }
}
```

### 2.4 实名认证
**接口地址**: `POST /user/verify`

**请求头**: `Authorization: Bearer {token}`

**请求参数**:
```json
{
  "realName": "真实姓名",
  "idCard": "身份证号",
  "idCardFrontUrl": "身份证正面照片URL",
  "idCardBackUrl": "身份证反面照片URL"
}
```

**返回数据**:
```json
{
  "code": 0,
  "data": {
    "verifyStatus": "pending"  // pending: 审核中, approved: 已通过, rejected: 已拒绝
  },
  "msg": "提交成功"
}
```

---

## 📦 3. 订单模块

### 3.1 创建订单
**接口地址**: `POST /orders/create`

**请求头**: `Authorization: Bearer {token}`

**请求参数**:
```json
{
  "serviceType": 1,  // 1-找售后，2-找售前，3-找团队
  "domainId": 1,  // 业务领域ID
  "title": "工单标题",
  "description": "详细描述",
  "images": ["图片URL1", "图片URL2"],
  "address": "服务地址",
  "contact": "联系人",
  "phone": "联系电话",
  "serviceDate": "2025-01-20",
  "remark": "备注（可选）"
}
```

**返回数据**:
```json
{
  "code": 0,
  "data": {
    "orderId": "订单ID",
    "orderNo": "订单号"
  },
  "msg": "订单创建成功"
}
```

### 3.2 获取订单列表
**接口地址**: `GET /orders/list`

**请求头**: `Authorization: Bearer {token}`

**查询参数**:
- `status` (可选): 订单状态（1-待付款，2-待接单，3-待签约，4-待服务，5-服务中，6-待确认，7-已完成，8-售后中，9-已取消）
- `page` (可选): 页码，默认1
- `pageSize` (可选): 每页数量，默认10

**返回数据**:
```json
{
  "code": 0,
  "data": {
    "list": [
      {
        "id": 1,
        "orderNo": "ORD202501190001",
        "status": 1,
        "statusText": "待付款",
        "serviceType": "售后服务",
        "title": "智慧园区弱电系统维修",
        "description": "网络设备故障，需要紧急维修",
        "address": "扬州市江都区智慧产业园3号楼",
        "engineerName": "",
        "price": "500.00",
        "createTime": "2025-01-19 10:30"
      }
    ],
    "total": 100,
    "page": 1,
    "pageSize": 10
  }
}
```

### 3.3 获取订单详情
**接口地址**: `GET /orders/{orderId}`

**请求头**: `Authorization: Bearer {token}`

**返回数据**:
```json
{
  "code": 0,
  "data": {
    "id": 1,
    "orderNo": "ORD202501190001",
    "status": 1,
    "statusText": "待付款",
    "serviceType": "售后服务",
    "title": "工单标题",
    "description": "详细描述",
    "images": ["图片URL"],
    "address": "服务地址",
    "contact": "联系人",
    "phone": "联系电话",
    "serviceDate": "2025-01-20",
    "remark": "备注",
    "price": "500.00",
    "engineerName": "工程师姓名（如有）",
    "createTime": "2025-01-19 10:30",
    "updateTime": "2025-01-19 10:30"
  }
}
```

### 3.4 取消订单
**接口地址**: `DELETE /orders/{orderId}`

**请求头**: `Authorization: Bearer {token}`

**返回数据**:
```json
{
  "code": 0,
  "msg": "订单已取消"
}
```

### 3.5 支付订单
**接口地址**: `POST /orders/{orderId}/pay`

**请求头**: `Authorization: Bearer {token}`

**请求参数**:
```json
{
  "paymentMethod": "wechat",  // 支付方式：wechat
  "amount": "500.00"
}
```

**返回数据**:
```json
{
  "code": 0,
  "data": {
    "paymentId": "支付订单ID",
    "paymentParams": {}  // 微信支付参数
  },
  "msg": "支付成功"
}
```

### 3.6 确认完成订单
**接口地址**: `POST /orders/{orderId}/complete`

**请求头**: `Authorization: Bearer {token}`

**返回数据**:
```json
{
  "code": 0,
  "msg": "订单已完成"
}
```

### 3.7 删除订单
**接口地址**: `DELETE /orders/{orderId}`

**请求头**: `Authorization: Bearer {token}`

**查询参数**:
- `action`: "delete"  // 删除操作

**返回数据**:
```json
{
  "code": 0,
  "msg": "订单已删除"
}
```

### 3.8 签约合同
**接口地址**: `POST /orders/{orderId}/sign`

**请求头**: `Authorization: Bearer {token}`

**请求参数**:
```json
{
  "signMethod": "electronic"  // 签约方式：electronic-电子签约
}
```

**返回数据**:
```json
{
  "code": 0,
  "data": {
    "contractUrl": "合同URL"
  },
  "msg": "签约成功"
}
```

### 3.9 申请售后
**接口地址**: `POST /orders/{orderId}/aftersale`

**请求头**: `Authorization: Bearer {token}`

**请求参数**:
```json
{
  "type": "refund",  // refund-退款, rework-返工, quality-质量问题
  "reason": "售后原因",
  "images": ["图片URL"]
}
```

**返回数据**:
```json
{
  "code": 0,
  "data": {
    "aftersaleId": "售后单ID"
  },
  "msg": "售后申请已提交"
}
```

### 3.10 获取订单状态统计
**接口地址**: `GET /orders/count`

**请求头**: `Authorization: Bearer {token}`

**返回数据**:
```json
{
  "code": 0,
  "data": {
    "pending": 3,  // 待接单
    "inService": 1,  // 服务中
    "toCheck": 0,  // 待验收
    "toRate": 0,  // 待评价
    "total": 4  // 总数
  }
}
```

---

## 👷 4. 工程师模块（工程师端）

### 4.1 更新在线状态
**接口地址**: `PUT /engineer/status`

**请求头**: `Authorization: Bearer {token}`

**请求参数**:
```json
{
  "isOnline": true  // true-在线，false-离线
}
```

**返回数据**:
```json
{
  "code": 0,
  "msg": "状态已更新"
}
```

### 4.2 获取可接订单列表
**接口地址**: `GET /engineer/orders/available`

**请求头**: `Authorization: Bearer {token}`

**查询参数**:
- `type` (可选): 订单类型（all-全部，piecework-计件工资，project-项目制，longterm-长期合作，urgent-紧急订单）
- `page` (可选): 页码
- `pageSize` (可选): 每页数量

**返回数据**:
```json
{
  "code": 0,
  "data": {
    "list": [
      {
        "id": 1,
        "title": "空调安装维修服务",
        "type": "计件工资",
        "orderType": "piecework",
        "address": "北京市朝阳区望京SOHO T3座",
        "time": "2025-11-10 09:00 - 18:00",
        "clientName": "张先生",
        "clientInfo": "需求明确·已发布2小时",
        "clientAvatar": "头像URL",
        "price": "800",
        "isNew": true,
        "isHot": false
      }
    ],
    "total": 50
  }
}
```

### 4.3 接单
**接口地址**: `POST /engineer/orders/{orderId}/accept`

**请求头**: `Authorization: Bearer {token}`

**返回数据**:
```json
{
  "code": 0,
  "msg": "接单成功"
}
```

### 4.4 获取工程师统计数据
**接口地址**: `GET /engineer/stats`

**请求头**: `Authorization: Bearer {token}`

**查询参数**:
- `month` (可选): 月份，格式 YYYY-MM，默认当前月

**返回数据**:
```json
{
  "code": 0,
  "data": {
    "monthlyIncome": "2,450.00",  // 本月收入
    "completedOrders": 12  // 完成订单数
  }
}
```

---

## 💬 5. 消息模块

### 5.1 获取消息列表
**接口地址**: `GET /messages`

**请求头**: `Authorization: Bearer {token}`

**查询参数**:
- `type` (可选): 消息类型（all-全部，order-订单消息，system-系统消息）
- `page` (可选): 页码
- `pageSize` (可选): 每页数量

**返回数据**:
```json
{
  "code": 0,
  "data": {
    "list": [
      {
        "id": 1,
        "type": "order",
        "icon": "📋",
        "title": "订单已确认",
        "content": "您的订单 OR202511180001 已被工程师确认接单，预计今天下午到达现场。",
        "time": "10分钟前",
        "isRead": false
      }
    ],
    "total": 50
  }
}
```

### 5.2 标记消息已读
**接口地址**: `POST /messages/{messageId}/read`

**请求头**: `Authorization: Bearer {token}`

**返回数据**:
```json
{
  "code": 0,
  "msg": "已标记为已读"
}
```

### 5.3 批量标记已读
**接口地址**: `POST /messages/read-all`

**请求头**: `Authorization: Bearer {token}`

**返回数据**:
```json
{
  "code": 0,
  "msg": "已全部标记为已读"
}
```

---

## 📤 6. 文件上传模块

### 6.1 上传文件
**接口地址**: `POST /upload`

**请求头**: 
- `Authorization: Bearer {token}`
- `Content-Type: multipart/form-data`

**请求参数**:
- `file`: 文件（图片）

**返回数据**:
```json
{
  "code": 0,
  "data": {
    "url": "文件访问URL",
    "filename": "文件名"
  },
  "msg": "上传成功"
}
```

---

## ⚠️ 7. 错误码说明

| 错误码 | 说明 |
|--------|------|
| 0 | 成功 |
| 401 | 未登录或token过期 |
| 403 | 无权限 |
| 400 | 请求参数错误 |
| 404 | 资源不存在 |
| 500 | 服务器错误 |

---

## 📝 8. 接口通用说明

### 8.1 请求头
- `Content-Type: application/json`（POST/PUT请求）
- `Authorization: Bearer {token}`（需要登录的接口）

### 8.2 响应格式
所有接口统一返回格式：
```json
{
  "code": 0,  // 状态码：0-成功，其他-失败
  "data": {},  // 数据
  "msg": "提示信息"
}
```

### 8.3 Token 验证
- 需要在请求头中携带 `Authorization: Bearer {token}`
- Token 过期返回 401，前端自动跳转到登录页

### 8.4 分页参数
列表接口统一使用：
- `page`: 页码，从1开始
- `pageSize`: 每页数量，默认10

---

## 🎯 9. 接口优先级

### 高优先级（核心功能）
1. ✅ 登录认证（微信登录、手机号登录）
2. ✅ 用户信息（获取、更新）
3. ✅ 订单管理（创建、列表、详情、取消）
4. ✅ 文件上传

### 中优先级（重要功能）
5. ⚠️ 订单支付
6. ⚠️ 订单状态操作（确认完成、签约、售后）
7. ⚠️ 消息系统
8. ⚠️ 实名认证

### 低优先级（扩展功能）
9. 📋 工程师模块（接单、在线状态）
10. 📋 用户统计数据
11. 📋 订单统计

---

## 📌 10. 待确认事项

1. **支付方式**: 目前只支持微信支付，是否需要支持其他支付方式？
2. **文件存储**: 图片文件存储方式（OSS/本地）？
3. **消息推送**: 是否需要实时消息推送（WebSocket）？
4. **电子签约**: 电子签约的第三方服务商？
5. **实名认证**: 是否需要对接第三方实名认证服务？
6. **订单匹配**: 工程师接单的匹配算法？

---

## 📅 11. 开发建议

1. **接口版本控制**: 建议使用 `/api/v1/` 作为接口前缀
2. **接口文档**: 建议使用 Swagger 生成接口文档
3. **接口测试**: 建议提供 Postman 测试集合
4. **错误日志**: 建议记录详细的错误日志，方便排查问题
5. **接口限流**: 建议对关键接口进行限流，防止恶意请求
6. **数据校验**: 建议前后端都进行数据校验，确保数据安全

---

*最后更新时间: 2025-01-19*

